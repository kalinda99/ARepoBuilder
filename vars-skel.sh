# This is a skel file, if you want to use it, fill in the variables and then rename it to vars.sh or copy the lines to that file.

export trash=true
export website=false
export sitedir=
export reponame=
export repodir=
export builddir=
export pkgs=
export port=
export address=""
export server=
export sshpath=
export gpgpkg=
export keychain=
export keyname=
export knockon=
export knockports=""
alias knock-ssh="knock -v -d 300 $server $knockports"
